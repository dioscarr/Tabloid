{
  "contractVersion": "1.0",
  "app": {
    "id": "app-gallery",
    "name": "App Gallery",
    "branch": "app-gallery",
    "description": "Approved application template selection and server-authorized provisioning requests."
  },
  "routes": [
    {
      "id": "admin-session",
      "direction": "outbound",
      "protocol": "https",
      "path": "/api/v1/session",
      "purpose": "Verify the current session and application-provisioning permission through the configured Admin API."
    },
    {
      "id": "admin-templates",
      "direction": "outbound",
      "protocol": "https",
      "path": "/api/v1/app-templates",
      "purpose": "List approved templates available to the current session through the configured Admin API."
    },
    {
      "id": "admin-provision",
      "direction": "outbound",
      "protocol": "https",
      "path": "/api/v1/applications/provision",
      "purpose": "Submit an idempotent server-side application provisioning request."
    }
  ],
  "content": {
    "editable": false,
    "surfaces": []
  }
}
