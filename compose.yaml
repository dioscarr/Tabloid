services:
  tabloid:
    build:
      context: .
      dockerfile: Containerfile
    image: localhost/tabloid:latest
    pull_policy: build
    container_name: tabloid
    restart: unless-stopped
    ports:
      - "${TABLOID_PORT:-8088}:8080"
