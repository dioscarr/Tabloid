{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check:shared-nav": "node scripts/verify-shared-nav.mjs",
    "check:brain-contract": "node scripts/verify-brain-contract.mjs",
    "preview": "vite preview",
    "serve": "vite build && vite preview --host 0.0.0.0 --port 5173 --strictPort"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "tailwindcss": "^4.3.3",
    "vite": "^8.2.0"
  }
}
