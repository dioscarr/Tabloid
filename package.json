{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "vite build && vite preview --host 0.0.0.0 --port 5173 --strictPort"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  }
}
