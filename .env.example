# Public defaults copied or generated into .env.local for development.
APP_BRANCH=admin
APP_SLUG=admin
VITE_APP_NAME=Admin
VITE_APP_ENV=preview
VITE_APP_URL=https://tabloid-admin-8c6976.tail70b7f1.ts.net/
VITE_API_BASE_URL=/api/v1
VITE_AUTH_BASE_URL=https://auth.tail70b7f1.ts.net/
VITE_ADMIN_WORKER_URL=https://dio.tail70b7f1.ts.net:9443/api/v1

# Secrets are deliberately absent. The provisioning worker resolves secret
# references server-side and injects them only into the process that needs them.
