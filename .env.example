# Public defaults copied or generated into .env.local for development.
APP_BRANCH=app-gallery
APP_SLUG=app-gallery
VITE_APP_NAME=App Gallery
VITE_APP_ENV=preview
VITE_APP_URL=

# Public origin of the private App Gallery worker. It serves only browser-safe
# routes: GET /api/v1/templates, POST /api/v1/apps, and
# GET /api/v1/app-requests. Never place credentials, tokens, or secrets in a
# VITE_ variable.
VITE_APP_GALLERY_WORKER_ORIGIN=https://dio.tail70b7f1.ts.net:9443
